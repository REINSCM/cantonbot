# Инструкция по развертыванию Mini App на сервере

## Для вашей структуры сервера

Если на сервере уже развернута собранная версия React приложения (видно по файлам `index.html`, `asset-manifest.json`, `static/`):

### Шаг 1: Создайте папку webapp

В той же директории, где находятся файлы `index.html`, `asset-manifest.json`, `static/`, создайте папку `webapp`:

**Через FTP/File Manager:**
- Создайте новую папку с именем `webapp`

**Через SSH:**
```bash
mkdir webapp
```

### Шаг 2: Загрузите файл index.html

Загрузите файл `index.html` в созданную папку `webapp/`:

**Структура должна быть такой:**
```
/
├── server/
├── static/
├── webapp/          ← создайте эту папку
│   └── index.html  ← загрузите файл сюда
├── index.html
├── asset-manifest.json
└── ...
```

### Шаг 3: Проверьте доступность

Откройте в браузере:
```
https://remindnation.tech/explorer/webapp/index.html
```

Должна загрузиться страница Mini App.

---

## Альтернативный вариант: Через исходники

Если у вас есть доступ к исходникам проекта на сервере:

### Шаг 1: Загрузите файл на сервер

Разместите файл `index.html` в папке:
```
/path/to/wlremind/client/public/webapp/index.html
```

### Шаг 2: Пересоберите React приложение

На сервере выполните:
```bash
cd /path/to/wlremind/client
npm run build
```

Это скопирует файл из `public/webapp/index.html` в `build/webapp/index.html`, который затем нужно будет загрузить на продакшен сервер.

### Шаг 2: Проверьте доступность

Откройте в браузере:
```
https://remindnation.tech/explorer/webapp/index.html
```

Должна загрузиться страница Mini App.

---

## Проверка работы

1. Откройте URL в браузере:
   ```
   https://remindnation.tech/explorer/webapp/index.html
   ```

2. Должна загрузиться страница с:
   - Заголовком "🔍 Canton Network Explorer"
   - Статистикой сети (Total CC, Validators, Transactions, Parties)
   - Кнопкой "🔄 Refresh Stats"
   - Встроенным iframe с Explorer

3. Проверьте в боте:
   - Откройте бота в Telegram
   - Нажмите кнопку "🌐 Explorer"
   - Mini App должен открыться прямо в Telegram

---

## Структура файлов на сервере

После развертывания структура должна быть такой:

**На продакшен сервере (где развернут сайт):**
```
/
├── server/
├── static/
├── webapp/          ← создайте эту папку
│   └── index.html  ← загрузите файл сюда
├── index.html
├── asset-manifest.json
└── ...
```

**В исходниках проекта (если используете вариант через исходники):**
```
/path/to/wlremind/
├── client/
│   ├── public/
│   │   └── webapp/
│   │       └── index.html  ← исходный файл
│   └── build/
│       └── webapp/
│           └── index.html  ← после сборки
└── server/
    └── index.js
```

---

## Важные замечания

1. **HTTPS обязателен** - Telegram Mini Apps работают только через HTTPS
2. **Права доступа** - Убедитесь, что файл доступен для чтения:
   ```bash
   chmod 644 /path/to/wlremind/client/build/webapp/index.html
   ```
3. **После обновления React приложения** - Если вы пересобираете проект, файл из `public/webapp/` автоматически скопируется в `build/webapp/`

---

## Решение проблем

### Файл не загружается (404 ошибка)

1. Проверьте путь: файл должен быть в `client/build/webapp/index.html`
2. Проверьте, что сервер раздает статику из `client/build/`
3. Проверьте права доступа к файлу

### Mini App не открывается в Telegram

1. Проверьте URL в `config.py` бота:
   ```python
   MINI_APP_URL = 'https://remindnation.tech/explorer/webapp/index.html'
   ```
2. Убедитесь, что URL доступен по HTTPS
3. Проверьте, что файл действительно существует на сервере

### Ошибки CORS

Если Mini App не может загрузить данные из API, проверьте настройки CORS на стороне API (обычно не требуется, так как API публичный).

