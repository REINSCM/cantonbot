# Canton Network Explorer - Telegram Mini App

Telegram Mini App для просмотра статистики Canton Network и доступа к Explorer.

## Развертывание

### Для вашей структуры сервера (уже развернутый сайт)

Если на сервере уже есть файлы `index.html`, `asset-manifest.json`, `static/`:

1. **Создайте папку `webapp`** в той же директории, где находятся эти файлы

2. **Загрузите файл `index.html`** в папку `webapp/`

3. **Структура должна быть:**
   ```
   /
   ├── server/
   ├── static/
   ├── webapp/          ← создайте
   │   └── index.html  ← загрузите сюда
   ├── index.html
   └── ...
   ```

4. Файл будет доступен по адресу: `https://remindnation.tech/explorer/webapp/index.html`

**Никаких изменений в бэкенде не требуется!** Файл будет раздаваться как статический.

📖 **Подробная инструкция:** см. `DEPLOY.md`

## Настройка бота

1. Убедитесь, что в `config.py` указан правильный URL Mini App
2. Перезапустите бота
3. Кнопка "🌐 Explorer" появится в клавиатуре бота

## Функции Mini App

- 📊 Отображение статистики сети (Total CC, Validators, Transactions, Parties)
- 🔄 Автоматическое обновление статистики каждые 30 секунд
- 🌐 Встроенный iframe с Explorer
- 🎨 Поддержка тем Telegram (светлая/темная)
- 📱 Адаптивный дизайн для мобильных устройств

## API

Mini App использует Canton Network Lighthouse API:
- `GET https://lighthouse.cantonloop.com/api/stats` - для получения статистики

## Безопасность

- Mini App работает только через HTTPS
- Все запросы к API идут напрямую с клиента (CORS должен быть настроен на сервере API)
- Telegram Web App API обеспечивает безопасность взаимодействия с ботом

