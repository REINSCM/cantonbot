# Canton Network Monitor Bot

Telegram бот для мониторинга активности в сети Canton Network и отслеживания цены токена CC/USDT.

## Возможности

- 📊 Получение статистики сети Canton Network
- 🔐 Просмотр списка валидаторов
- 🔄 Мониторинг раундов и governance
- 💸 Детальная информация о транзакциях и партиях
- 💰 Автоматическая отправка актуальной цены CC/USDT в Telegram канал
- 📈 Отслеживание цены CC/USDT с биржи Bybit

## Установка

1. Клонируйте репозиторий или скачайте файлы проекта

2. Установите зависимости:

   **Windows:**
   ```bash
   install.bat
   ```
   или вручную:
   ```bash
   py -m pip install -r requirements.txt
   ```

   **Linux/Mac:**
   ```bash
   pip install -r requirements.txt
   ```
   или
   ```bash
   python3 -m pip install -r requirements.txt
   ```

3. Создайте файл `.env` на основе `.env.example`:

   **Windows (PowerShell):**
   ```powershell
   Copy-Item .env.example .env
   ```

   **Linux/Mac:**
   ```bash
   cp .env.example .env
   ```

4. Заполните `.env` файл:
   - `TELEGRAM_BOT_TOKEN` - токен вашего Telegram бота (получите у [@BotFather](https://t.me/BotFather))
   - `TELEGRAM_CHANNEL_ID` - ID вашего Telegram канала
     - Для публичного канала: `@your_channel` (например: `@my_channel`)
     - Для приватного канала: числовой ID (например: `-1001234567890`)
     - Чтобы получить ID канала: добавьте бота [@userinfobot](https://t.me/userinfobot) в канал и отправьте любое сообщение
     - **Важно:** Бот должен быть добавлен в канал как администратор!

5. (Опционально) Проверьте доступ к каналу:
   ```bash
   py check_channel.py
   ```
   или
   ```bash
   .venv\Scripts\python.exe check_channel.py
   ```
   Скрипт проверит доступ бота к каналу и отправит тестовое сообщение.

## Запуск

**Windows:**
```bash
start.bat
```
или вручную:
```bash
py main.py
```

**Linux/Mac:**
```bash
python main.py
```
или
```bash
python3 main.py
```

## Команды бота

### Основные команды

- `/start` - Начать работу с ботом
- `/help` - Справка по командам
- `/stats` - Статистика сети Canton Network
- `/validators` - Список валидаторов
- `/rounds [page] [limit]` - Список раундов (по умолчанию: page=1, limit=20)
- `/governance [page] [limit]` - Список governance
- `/price` - Текущая цена CC/USDT

### Команды с параметрами

- `/governance_id <id>` - Детали governance по ID
- `/party <id>` - Информация о партии по ID
- `/party_tx <id> [limit]` - Транзакции партии (по умолчанию limit=20)
- `/party_transfers <id> [limit]` - Трансферы партии (по умолчанию limit=20)

### Примеры использования

```
/governance_id 123
/party party123
/party_tx party123 50
/party_transfers party123 30
```

## API Endpoints

Бот использует следующие эндпоинты Canton Network Lighthouse API:

- `GET /stats` - статистика сети
- `GET /validators` - список валидаторов
- `GET /rounds?page=&limit=` - список раундов
- `GET /governance?page=&limit=` - список governance
- `GET /governance/:id` - детали governance
- `GET /transactions/:id` - детали транзакции
- `GET /parties/:id` - информация о партии
- `GET /parties/:id/tx?limit=` - транзакции партии
- `GET /parties/:id/transfers?limit=` - трансферы партии

Базовый URL: `https://lighthouse.cantonloop.com/api`

## Автоматическая отправка цены

Бот автоматически отправляет актуальную цену CC/USDT в указанный Telegram канал каждые 5 минут (настраивается в `config.py` через `PRICE_UPDATE_INTERVAL`).

## Структура проекта

```
cantonbot/
├── main.py              # Основной файл бота
├── canton_api.py        # Модуль для работы с Canton Network API
├── price_fetcher.py     # Модуль для получения цены CC/USDT
├── config.py            # Конфигурация
├── requirements.txt     # Зависимости
├── install.bat          # Скрипт установки зависимостей (Windows)
├── start.bat            # Скрипт запуска бота (Windows)
├── .env                 # Переменные окружения (создайте сами)
├── .env.example         # Пример файла .env
└── README.md            # Документация
```

## Требования

- Python 3.8+
- Telegram Bot Token
- Telegram Channel ID (для автоматической отправки цены)

## Лицензия

MIT

